name: structured_strategy_edge_do_mod_begin_final_value
section: current-strategy-edge-do-mod
tier: stable
kind: law
features: begin, sequence, strategy
compiler-path: codegen.sequence, reader.ast_begin, runner.strategy_arg, runner.typed_generator
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.begin, atom.control.do, atom.core.equality, atom.core.plus
related-properties: signal_atom_do_last_value, signal_atom_equality_reflexive, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int@edge+do+mod b:Int@tiny+mod p:Bool@logic+if
type: Bool
expect: True
description: V10 strategy property begin_final_value generated with edge_do_mod arguments to stress compiler dispatch, envs, and PHI lowering.
law: (= (begin {p} {a} (+ {a} {b})) (+ {a} {b}))
