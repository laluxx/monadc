name: structured_strategy_edge_if_with_if_substitution_branch
section: current-strategy-edge-if-with
tier: stable
kind: law
features: if, phi, arithmetic, strategy
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.if_phi, infer.arithmetic, infer.if_phi, reader.ast_if, runner.strategy_arg, runner.typed_generator
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.if, atom.core.equality, atom.core.minus, atom.core.plus
related-properties: signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int@edge+if+with b:Int@edge+do p:Bool@logic+if
type: Bool
expect: True
description: V10 strategy property if_substitution_branch generated with edge_if_with arguments to stress compiler dispatch, envs, and PHI lowering.
law: (= (if {p} (- (+ {a} {b}) {b}) {a}) {a})
