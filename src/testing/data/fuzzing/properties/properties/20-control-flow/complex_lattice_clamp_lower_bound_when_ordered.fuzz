name: complex_lattice_clamp_lower_bound_when_ordered
section: lattice-clamp
tier: stable
kind: law
features: arithmetic, if, lattice, logic, phi, predicate, when
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.if_phi, codegen.if_void, codegen.logic_dispatch, codegen.predicate_dispatch, desugar.when, infer.arithmetic, infer.if_phi, infer.logic, infer.type_predicate, reader.ast_if
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.if, atom.infer.predicate, atom.logic.and, atom.logic.not, atom.logic.or
related-properties: signal_atom_if_same_branch_eliminates
args: x:Int lo:Int hi:Int
type: Bool
expect: True
description: When lo <= hi, clamping with max then min must never fall below lo.
law: (if (fuzz-lte? {lo} {hi}) (fuzz-lte? {lo} (fuzz-min (fuzz-max {x} {lo}) {hi})) True)
