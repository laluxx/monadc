name: structured_dispatch_direct_plus_matches_helper_after_do
section: dispatch-matrix-current
tier: stable
kind: law
features: arithmetic, dispatch, do, equality, logic, set
compiler-path: codegen.arithmetic_dispatch, codegen.equality_dispatch, codegen.logic_dispatch, codegen.runtime_call, codegen.sequence, infer.arithmetic, infer.equality, infer.logic, reader.ast_do, runtime.set
signal: medium
corpus-role: semantic-law
uses-atoms: atom.codegen.dispatch, atom.control.do, atom.core.equality, atom.core.plus, atom.logic.and, atom.logic.not, atom.logic.or
related-properties: signal_atom_do_last_value, signal_atom_equality_reflexive, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int b:Int setup:Bool
type: Bool
expect: True
description: Stress direct operator and predicate dispatch in expression, branch, and operand positions.
law: (= (+ (do {setup} {a}) (do {setup} {b})) (fuzz-add {a} {b}))
