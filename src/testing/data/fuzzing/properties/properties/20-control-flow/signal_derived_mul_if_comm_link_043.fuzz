name: signal_derived_mul_if_comm_link_043
section: current-atom-derived
tier: stable
kind: law
features: if, phi, *, equality, logic
compiler-path: codegen.expr.if, codegen.expr.binary-mul, codegen.phi.int
signal: medium
corpus-role: derived-atom-linked
uses-atoms: atom.core.multiply, atom.core.multiply.commutative, atom.control.if, atom.core.equality
depends-on: atom.core.multiply.commutative, atom.control.if.idempotent
related-properties: signal_atom_multiply_commutative, signal_atom_if_same_branch_eliminates
args: a:Int@tiny+if b:Int@tiny+with flag:Bool@logic+if
type: Bool
expect: True
description: Derived property that explicitly links a real language/compiler path to foundational atom properties.
law: (= (* (if {flag} {a} {b}) {b}) (* {b} (if {flag} {a} {b})))
