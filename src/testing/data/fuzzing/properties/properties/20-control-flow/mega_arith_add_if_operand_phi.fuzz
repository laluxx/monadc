name: mega_arith_add_if_operand_phi
section: codegen-arith-phi
tier: stable
kind: law
features: arithmetic, codegen, equality, if, logic, phi
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.equality_dispatch, codegen.if_phi, codegen.logic_dispatch, infer.arithmetic, infer.equality, infer.if_phi, infer.logic, reader.ast_if
signal: medium
corpus-role: semantic-law
uses-atoms: atom.codegen.dispatch, atom.control.if, atom.core.equality, atom.core.plus, atom.logic.and, atom.logic.not, atom.logic.or
related-properties: signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: p:Bool a:Int b:Int c:Int
type: Bool
expect: True
description: Arithmetic + must compose with scalar PHI values produced by if.
law: (= (+ (if {p} {a} {b}) {c}) (if {p} (+ {a} {c}) (+ {b} {c})))
