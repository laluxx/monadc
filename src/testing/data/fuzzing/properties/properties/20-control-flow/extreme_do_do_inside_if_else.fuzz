name: extreme_do_do_inside_if_else
section: control-do-deep
tier: stable
kind: law
features: arithmetic, control, do, equality, if, phi, set
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.equality_dispatch, codegen.if_phi, codegen.runtime_call, codegen.sequence, infer.arithmetic, infer.equality, infer.if_phi, reader.ast_do, reader.ast_if, runtime.set
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.do, atom.control.if, atom.core.equality
related-properties: signal_atom_do_last_value, signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates
args: setup:Bool cond:Bool a:Int b:Int
type: Bool
expect: True
description: Sequencing must return the last expression and compose inside branches, comparisons, and bindings.
law: (= (if {cond} {a} (do {setup} {b})) (if {cond} {a} {b}))
