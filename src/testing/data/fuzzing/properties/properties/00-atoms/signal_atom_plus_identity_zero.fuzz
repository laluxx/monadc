name: signal_atom_plus_identity_zero
section: current-atom-foundation
tier: stable
kind: law
features: arithmetic, +, identity, equality, logic
compiler-path: codegen.expr.binary-add, runtime.int.add
signal: medium
corpus-role: atom-foundation
atom: atom.core.plus.identity.zero
proves: atom.core.plus.identity.zero
uses-atoms: atom.core.equality, atom.core.plus, atom.logic.and
args: a:Int@edge+if+with
type: Bool
expect: True
description: Foundation atom proof for atom.core.plus.identity.zero; derived properties link through uses-atoms/depends-on metadata.
law: (and (= (+ {a} 0) {a}) (= (+ 0 {a}) {a}))
