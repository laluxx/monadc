#ifndef MONAD_QTT_EFFECT_COMPAT_H
#define MONAD_QTT_EFFECT_COMPAT_H

/* Compatibility include during the effect-subsystem extraction.  New code
 * should include effects/effect.h; QTT owns the integration, not the row
 * algebra implementation. */
#include "../effects/effect.h"

#endif
