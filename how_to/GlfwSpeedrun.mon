include <GLFW/glfw3.h>

glfwInit

define window
glfwCreateWindow 640 480 "Type inference over C ?!" nil nil

until glfwWindowShouldClose window
  glfwSwapBuffers window
  glfwPollEvents

glfwDestroyWindow window
glfwTerminate
