;;; RunDonut.mon --- Render the spinning donut in a terminal

:author "Laluxx"
:version "1.0.0"
:keywords "cli terminal graphics donut example"

;;; Commentary:

 Donut.mon is now directly executable and renders a frame by default. This
 compatibility wrapper only imports it, preserving the older RunDonut command.

;;; Code:

import Donut

module RunDonut []
