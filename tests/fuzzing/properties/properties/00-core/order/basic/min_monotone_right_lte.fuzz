name: min_monotone_right_lte
section: order
args: a:Int b:Int c:Int
type: Bool
expect: True
description: Min must be monotone in its right operand under less-or-equal.
law: (if (fuzz-lte? {a} {b}) (fuzz-lte? (fuzz-min {c} {a}) (fuzz-min {c} {b})) True)
