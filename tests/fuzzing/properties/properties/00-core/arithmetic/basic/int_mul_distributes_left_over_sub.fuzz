name: int_mul_distributes_left_over_sub
section: arithmetic
args: a:Int b:Int c:Int
type: Bool
expect: True
description: A left-side factor must distribute over subtraction.
law: (= (fuzz-mul {a} (fuzz-sub {b} {c})) (fuzz-sub (fuzz-mul {a} {b}) (fuzz-mul {a} {c})))
