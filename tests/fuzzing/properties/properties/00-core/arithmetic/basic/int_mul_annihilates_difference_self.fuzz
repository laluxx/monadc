name: int_mul_annihilates_difference_self
section: arithmetic
args: a:Int b:Int
type: Bool
expect: True
description: Multiplying by a self-difference must produce zero.
law: (= (fuzz-mul {a} (fuzz-sub {b} {b})) 0)
