name: structured_v11_if_phi_same_add_r08
section: control-phi-v11
tier: stable
kind: law
features: if, phi, arithmetic, strategy
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.if_phi, infer.arithmetic, infer.if_phi, reader.ast_if, runner.strategy_arg, runner.typed_generator
uses-atoms: atom.binding.with, atom.control.if, atom.core.equality, atom.core.plus, atom.runtime.array
related-properties: signal_v13_atom_array_index_valid, signal_v13_atom_equality_reflexive, signal_v13_atom_if_same_branch_eliminates, signal_v13_atom_plus_associative, signal_v13_atom_plus_commutative, signal_v13_atom_plus_identity_zero, signal_v13_atom_with_substitution
signal: medium
corpus-role: semantic-law
args: a:Int@edge+if b:Int@edge+do p:Bool@logic+if
type: Bool
expect: True
description: V11 stable compiler-path law if_phi_same_add variant 8; uses strategy args and explicit compiler-path metadata for feature coverage.
law: (with [unused 1] (= (if {p} (+ {a} {b}) (+ {a} {b})) (+ {a} {b})))
