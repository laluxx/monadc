name: structured_v11_if_phi_commuted_sum_r03
section: control-phi-v11
tier: stable
kind: law
features: if, phi, arithmetic, comparison, strategy
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.comparison_dispatch, codegen.if_phi, infer.arithmetic, infer.comparison, infer.if_phi, reader.ast_if, runner.strategy_arg, runner.typed_generator
uses-atoms: atom.control.do, atom.control.if, atom.core.equality, atom.core.plus
related-properties: signal_v13_atom_do_last_value, signal_v13_atom_equality_reflexive, signal_v13_atom_if_same_branch_eliminates, signal_v13_atom_plus_associative, signal_v13_atom_plus_commutative, signal_v13_atom_plus_identity_zero
signal: medium
corpus-role: semantic-law
args: a:Int@edge+if b:Int@edge+if p:Bool@logic+if
type: Bool
expect: True
description: V11 stable compiler-path law if_phi_commuted_sum variant 3; uses strategy args and explicit compiler-path metadata for feature coverage.
law: (do 0 (= (+ (if {p} {a} {b}) (if {p} {b} {a})) (+ {a} {b})))
