name: compiler_deep_v8_logic_and_if_encoded_truth_table
section: logic-coercion-v8
args: p:Bool q:Bool
type: Bool
expect: True
description: Nested if should encode Bool conjunction without relying on primitive and syntax.
law: (= (if {p} {q} False) (if {q} {p} False))
