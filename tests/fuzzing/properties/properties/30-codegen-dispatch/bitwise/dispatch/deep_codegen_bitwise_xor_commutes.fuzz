name: deep_codegen_bitwise_xor_commutes
section: codegen-bitwise-dispatch
args: a:Int b:Int
type: Bool
expect: True
description: bit-xor must commute for generated operands.
law: (= (bit-xor {a} {b}) (bit-xor {b} {a}))
