name: structured_v9_dispatch_direct_mul_matches_helper_in_branch
section: dispatch-matrix-v9
args: p:Bool a:Int b:Int c:Int
type: Bool
expect: True
description: Stress direct operator and predicate dispatch in expression, branch, and operand positions.
law: (= (if {p} (* {a} {b}) (* {a} {c})) (* {a} (if {p} {b} {c})))
