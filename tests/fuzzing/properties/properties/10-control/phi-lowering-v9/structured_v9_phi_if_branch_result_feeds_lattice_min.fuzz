name: structured_v9_phi_if_branch_result_feeds_lattice_min
section: control-phi-v9
args: p:Bool a:Int b:Int c:Int
type: Bool
expect: True
description: Stress branch PHI lowering and nested conditional codegen.
law: (= (fuzz-min (if {p} {a} {b}) {c}) (if {p} (fuzz-min {a} {c}) (fuzz-min {b} {c})))
