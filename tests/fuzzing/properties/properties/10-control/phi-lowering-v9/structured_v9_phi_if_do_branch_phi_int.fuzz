name: structured_v9_phi_if_do_branch_phi_int
section: control-phi-v9
args: p:Bool a:Int b:Int setup:Int
type: Bool
expect: True
description: Stress branch PHI lowering and nested conditional codegen.
law: (= (if {p} (do {setup} {a}) (do {setup} {b})) (if {p} {a} {b}))
