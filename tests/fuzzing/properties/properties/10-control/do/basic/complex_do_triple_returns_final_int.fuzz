name: complex_do_triple_returns_final_int
section: control-do
args: a:Int b:Bool c:Int
type: Bool
expect: True
description: A three-expression do form with mixed types must return the final Int expression.
law: (= (do {a} {b} {c}) {c})
