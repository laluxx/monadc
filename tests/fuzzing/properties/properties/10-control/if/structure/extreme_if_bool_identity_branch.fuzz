name: extreme_if_bool_identity_branch
section: control-if-structure
args: cond:Bool p:Bool q:Bool
type: Bool
expect: True
description: Nested if structure must preserve branch semantics and boolean reconstruction.
law: (= (if (if {cond} True False) {p} {q}) (if {cond} {p} {q}))
