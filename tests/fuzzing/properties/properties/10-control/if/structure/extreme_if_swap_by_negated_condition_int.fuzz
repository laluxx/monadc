name: extreme_if_swap_by_negated_condition_int
section: control-if-structure
args: cond:Bool a:Int b:Int
type: Bool
expect: True
description: Nested if structure must preserve branch semantics and boolean reconstruction.
law: (= (if (if {cond} False True) {a} {b}) (if {cond} {b} {a}))
