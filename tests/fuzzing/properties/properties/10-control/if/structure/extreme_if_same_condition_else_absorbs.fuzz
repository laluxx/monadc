name: extreme_if_same_condition_else_absorbs
section: control-if-structure
args: cond:Bool a:Int b:Int c:Int
type: Bool
expect: True
description: Nested if structure must preserve branch semantics and boolean reconstruction.
law: (= (if {cond} {a} (if {cond} {b} {c})) (if {cond} {a} {c}))
