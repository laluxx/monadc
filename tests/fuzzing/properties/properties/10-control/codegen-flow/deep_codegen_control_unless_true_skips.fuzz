name: deep_codegen_control_unless_true_skips
section: codegen-control-flow
args: a:Int b:Int
type: Bool
expect: True
description: unless True must skip body and preserve the bound variable.
law: (= (with [x {a}] (do (unless True (set! x {b})) x)) {a})
