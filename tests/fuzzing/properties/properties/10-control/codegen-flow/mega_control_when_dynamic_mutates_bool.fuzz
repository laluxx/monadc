name: mega_control_when_dynamic_mutates_bool
section: codegen-control-flow
args: p:Bool q:Bool
type: Bool
expect: True
description: when must gate Bool mutation and branch value.
law: (= (with [x {p}] (do (when {q} (set! x (not x))) x)) (if {q} (not {p}) {p}))
