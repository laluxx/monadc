name: structured_v9_loop_while_condition_reads_mutated_bool
section: control-loop-mutation-v9
args: p:Bool
type: Bool
expect: True
description: Stress lowering of loops, mutable locals, nested blocks, and branch guarded mutation.
law: (= (with [guard {p} hits 0] (do (while guard (begin (set! hits (+ hits 1)) (set! guard False))) hits)) (if {p} 1 0))
