name: deep_codegen_array_array_concat_count
section: codegen-array-indexing
args: a:Int b:Int
type: Bool
expect: True
description: ++ over arrays must produce an array with combined count.
law: (= (count (++ [{a}] [{b}])) 2)
