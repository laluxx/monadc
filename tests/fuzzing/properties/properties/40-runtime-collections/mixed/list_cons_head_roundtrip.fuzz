name: list_cons_head_roundtrip
section: collections
args: a:Int
type: Bool
expect: True
description: head on a one-element cons list must recover the generated value.
law: (= (head (cons {a} (list))) {a})
