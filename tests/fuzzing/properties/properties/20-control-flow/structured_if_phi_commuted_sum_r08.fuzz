name: structured_if_phi_commuted_sum_r08
section: control-phi-current
tier: stable
kind: law
features: if, phi, arithmetic, comparison, strategy
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.comparison_dispatch, codegen.if_phi, infer.arithmetic, infer.comparison, infer.if_phi, reader.ast_if, runner.strategy_arg, runner.typed_generator
signal: medium
corpus-role: semantic-law
uses-atoms: atom.binding.with, atom.control.if, atom.core.equality, atom.core.plus, atom.runtime.array
related-properties: signal_atom_array_index_valid, signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero, signal_atom_with_substitution
args: a:Int@edge+if b:Int@edge+if p:Bool@logic+if
type: Bool
expect: True
description: V11 stable compiler-path law if_phi_commuted_sum variant 8; uses strategy args and explicit compiler-path metadata for feature coverage.
law: (with [unused 1] (= (+ (if {p} {a} {b}) (if {p} {b} {a})) (+ {a} {b})))
