name: structured_strategy_edge_do_mod_array_index_phi_value
section: current-strategy-edge-do-mod
tier: stable
kind: law
features: array, index, if, phi, strategy
compiler-path: codegen.array_literal, codegen.branch_phi, codegen.if_phi, infer.array, infer.if_phi, reader.array_literal, reader.ast_if, runner.strategy_arg, runner.typed_generator
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.if, atom.core.equality, atom.core.plus, atom.runtime.array
related-properties: signal_atom_array_index_valid, signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int@edge+do+mod b:Int@tiny+mod p:Bool@logic+if
type: Bool
expect: True
description: V10 strategy property array_index_phi_value generated with edge_do_mod arguments to stress compiler dispatch, envs, and PHI lowering.
law: (= ([{a} {b} (+ {a} {b})] (if {p} 0 1)) (if {p} {a} {b}))
