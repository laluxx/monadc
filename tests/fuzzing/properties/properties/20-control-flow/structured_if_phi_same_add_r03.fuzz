name: structured_if_phi_same_add_r03
section: control-phi-current
tier: stable
kind: law
features: if, phi, arithmetic, strategy
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.if_phi, infer.arithmetic, infer.if_phi, reader.ast_if, runner.strategy_arg, runner.typed_generator
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.do, atom.control.if, atom.core.equality, atom.core.plus
related-properties: signal_atom_do_last_value, signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int@edge+if b:Int@edge+do p:Bool@logic+if
type: Bool
expect: True
description: V11 stable compiler-path law if_phi_same_add variant 3; uses strategy args and explicit compiler-path metadata for feature coverage.
law: (do 0 (= (if {p} (+ {a} {b}) (+ {a} {b})) (+ {a} {b})))
