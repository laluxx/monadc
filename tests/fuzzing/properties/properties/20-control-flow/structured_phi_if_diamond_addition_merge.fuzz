name: structured_phi_if_diamond_addition_merge
section: control-phi-current
tier: stable
kind: law
features: arithmetic, control, equality, if, phi
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.equality_dispatch, codegen.if_phi, infer.arithmetic, infer.equality, infer.if_phi, reader.ast_if
signal: medium
corpus-role: semantic-law
uses-atoms: atom.control.if, atom.core.equality, atom.core.plus
related-properties: signal_atom_equality_reflexive, signal_atom_if_same_branch_eliminates, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: p:Bool a:Int b:Int c:Int
type: Bool
expect: True
description: Stress branch PHI lowering and nested conditional codegen.
law: (= (+ (if {p} {a} {b}) (if {p} {c} {c})) (if {p} (+ {a} {c}) (+ {b} {c})))
