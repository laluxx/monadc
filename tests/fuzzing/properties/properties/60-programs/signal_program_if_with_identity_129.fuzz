name: signal_program_if_with_identity_129
section: current-program-semantics
tier: stable
kind: program
features: program, if, with, +, identity, phi, env, equality
compiler-path: program.module.body, codegen.expr.if, codegen.binding.with, codegen.expr.binary-add
signal: high
corpus-role: program-semantics
uses-atoms: atom.control.if, atom.binding.with, atom.core.plus.identity.zero, atom.core.equality
depends-on: atom.core.plus.identity.zero, atom.binding.with.substitution
args: x:Int@edge+if+with y:Int@edge+do flag:Bool@logic+if
type: Bool
expect: True
description: Program-kind property: source contains its own show and links real program codegen to foundational atoms.
program: (show (= (with [a (if {flag} {x} {y})] (+ a 0)) (if {flag} {x} {y})))
