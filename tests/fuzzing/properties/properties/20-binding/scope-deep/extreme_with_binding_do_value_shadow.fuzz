name: extreme_with_binding_do_value_shadow
section: binding-scope-deep
args: setup:Bool outer:Int inner:Int
type: Bool
expect: True
description: Lexical binding and shadowing must preserve dependency order and branch-local scope.
law: (with [x {outer}] (= (do {setup} (with [x {inner}] x)) {inner}))
