name: deep_codegen_runtime_head_list_matches_car
section: codegen-runtime-values
args: a:Int
type: Bool
expect: True
description: head on list must match car.
law: (= (head (cons {a} (list))) (car (cons {a} (list))))
