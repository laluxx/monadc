name: extreme_arith_nested_helper_direct_mix
section: arithmetic-normalization-deep
tier: stable
kind: law
features: arithmetic, equality, logic
compiler-path: codegen.arithmetic_dispatch, codegen.equality_dispatch, codegen.logic_dispatch, infer.arithmetic, infer.equality, infer.logic
signal: medium
corpus-role: semantic-law
uses-atoms: atom.core.equality, atom.core.plus, atom.logic.and, atom.logic.not, atom.logic.or
related-properties: signal_atom_equality_reflexive, signal_atom_plus_associative, signal_atom_plus_commutative, signal_atom_plus_identity_zero
args: a:Int b:Int c:Int
type: Bool
expect: True
description: Deep arithmetic normalization must preserve integer semantics across direct codegen and helper calls.
law: (= (fuzz-add (+ {a} {b}) (fuzz-sub {c} {a})) (+ {b} {c}))
