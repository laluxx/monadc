name: signal_stress_string_if_contains_018
section: current-stress
tier: stress
kind: program
features: stress, program, string, contains, if, dependent-arg
compiler-path: stress.program, codegen.string.literal, runtime.string.contains, codegen.expr.if
signal: high
corpus-role: stress-program
uses-atoms: atom.runtime.string.contains.substring, atom.control.if
depends-on: atom.runtime.string.contains.substring, atom.control.if.idempotent
args: s:NonEmptyString@escape needle:SubstrOf(s) flag:Bool@logic
type: Bool
expect: True
description: Stress program property combining several compiler stages while linked to foundational atoms.
program: (show (if {flag} (contains? {s} {needle}) (contains? {s} {needle})))
