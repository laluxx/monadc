name: structured_v9_cross_with_loop_mutation_branch_set_contains
section: cross-compiler-v9
args: p:Bool a:Int
type: Bool
expect: True
description: Stress cross-feature lowering across control flow, env lookup, runtime collections, predicates, quote/null, and operator dispatch.
law: (= (with [s (set) i 0] (do (while (< i 2) (begin (when (if (= i 0) {p} False) (conj! s {a})) (set! i (+ i 1)))) (contains? s {a}))) {p})
