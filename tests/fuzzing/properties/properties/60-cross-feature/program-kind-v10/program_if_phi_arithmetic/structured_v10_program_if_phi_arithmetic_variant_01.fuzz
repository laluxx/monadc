name: structured_v10_program_if_phi_arithmetic_variant_01
section: v10-program-kind
tier: stable
kind: program
features: program, if, phi, arithmetic
compiler-path: codegen.arithmetic_dispatch, codegen.branch_phi, codegen.if_phi, codegen.module_body, infer.arithmetic, infer.if_phi, reader.ast_if, runner.program_kind
uses-atoms: atom.control.if, atom.core.equality, atom.core.plus
related-properties: signal_v13_atom_equality_reflexive, signal_v13_atom_if_same_branch_eliminates, signal_v13_atom_plus_associative, signal_v13_atom_plus_commutative, signal_v13_atom_plus_identity_zero
signal: high
corpus-role: program-semantics
args: a:Int@edge+if b:Int@edge+do p:Bool@logic
type: Bool
expect: True
description: V10 program-kind property variant 1 for program_if_phi_arithmetic, checking full program source handling rather than show-wrapped law handling.
program: (show (= (if {p} (+ {a} {b}) (+ {a} {b})) (+ {a} {b})))
