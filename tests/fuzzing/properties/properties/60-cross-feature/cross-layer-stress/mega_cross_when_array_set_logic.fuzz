name: mega_cross_when_array_set_logic
section: codegen-cross-layer-stress
args: p:Bool a:Int b:Int
type: Bool
expect: True
description: when + array-as-function + mutating set + contains? must compose.
law: (= (with [xs [{a} {b}] s (set)] (do (when {p} (conj! s (xs 1))) (contains? s {b}))) {p})
