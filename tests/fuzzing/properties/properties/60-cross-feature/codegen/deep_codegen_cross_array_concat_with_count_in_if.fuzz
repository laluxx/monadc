name: deep_codegen_cross_array_concat_with_count_in_if
section: codegen-cross-feature
args: p:Bool a:Int b:Int
type: Bool
expect: True
description: if PHI over array values must preserve count.
law: (= (count (if {p} (++ [{a}] [{b}]) [{a} {b}])) 2)
