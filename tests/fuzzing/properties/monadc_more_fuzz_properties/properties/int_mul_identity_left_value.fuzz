name: int_mul_identity_left_value
section: arithmetic
args: a:Int
type: Bool
expect: True
description: Multiplying by one on the left must preserve the generated Int expression exactly.
law: (= (fuzz-mul 1 {a}) {a})
