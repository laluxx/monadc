name: complex_if_branchwise_mul_same_condition
section: control-if-arithmetic
args: cond:Bool a:Int b:Int c:Int d:Int
type: Bool
expect: True
description: Multiplying two if expressions with the same condition must choose the branchwise product.
law: (= (fuzz-mul (if {cond} {a} {b}) (if {cond} {c} {d})) (if {cond} (fuzz-mul {a} {c}) (fuzz-mul {b} {d})))
