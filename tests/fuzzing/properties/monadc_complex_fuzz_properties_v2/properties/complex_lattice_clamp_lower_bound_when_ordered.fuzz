name: complex_lattice_clamp_lower_bound_when_ordered
section: lattice-clamp
args: x:Int lo:Int hi:Int
type: Bool
expect: True
description: When lo <= hi, clamping with max then min must never fall below lo.
law: (if (fuzz-lte? {lo} {hi}) (fuzz-lte? {lo} (fuzz-min (fuzz-max {x} {lo}) {hi})) True)
