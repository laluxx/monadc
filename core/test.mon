
;; x :: Int
;; x = 30



;; [x :: Int]
;; y = 30


;; x :: Int = 30
;; [x :: Int] = 30
;; y = 30.0  ; inferred as Float, this is a top level variable


;; define double :: Int -> Int
;;   x -> (def y 30)
;;        x * 2

;; define double' :: Int -> Int
;;   x -> y :: Int = 30
;;        x y z :: Int = 30
;;        x * 2

;; show double 3


define case-delta 'a' - 'A'

define char-upcase :: Char -> Char
  c | >= 'a' and <= 'z' -> c - case-delta
    | otherwise         -> c
  :doc "Convert C to upper case."


show char-upcase 'a'
